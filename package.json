{
  "name": "win-hello",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite --port 8080 --host",
    "build": "vite build",
    "preview": "vite preview --port 8080 --host"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
